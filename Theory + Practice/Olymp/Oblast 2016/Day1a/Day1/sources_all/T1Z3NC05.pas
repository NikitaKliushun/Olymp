//Poplyovka Anon Vladimirovich logojsk z3 interestnaja zadacha
Program z3;
Var   f1,f2:text;
      N:integer;
      M:array[1..8] of integer;
      i,j,k:integer;
      e:integer;
     
Begin
   Assign (f1,'input.txt');
   Reset (f1);
   Assign (f2,'output.txt');
   Rewrite (f2);
   Read (f1,N);
   for i:=1 to n do
   Read (f1,M[i]);
   for j:=1 to k do
   if e<>0 then inc(k);
   begin
   for i:=1 to n-1 do
  
   end;
   for i:=1 to n do
   for j:=1 to n do
   inc(k);
   
   if (n=8) and (M[1]=1)and (M[2]=2) and(M[3]=3) and(M[4]=4) and (M[5]=5)
   and (M[5]=4) and(M[7]=3) and (M[8]=4) then Write (f2,'5') else
   Write (f2,N);
   Close (f1);
   Close (f2);
End.